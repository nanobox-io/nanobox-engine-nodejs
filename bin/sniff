#!/bin/bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = code_dir

[[ -f ${1}/package.json ]] && exit 0
[[ -f ${1}/server.js ]] && exit 0
[[ -f ${1}/app.js ]] && exit 0

exit 1
